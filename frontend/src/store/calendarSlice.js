import { createSlice } from "@reduxjs/toolkit";

const caledarSlice = createSlice({
  name: "calendar",
  initialState: {},
  reducers: {
    
  },
});

export const {  } = caledarSlice.actions;

export default caledarSlice.reducer;
